name: ping streamlit app 

on:
  schedule:
    - cron: '*/10 * * * *' # run every 10 mins 

jobs:
  keep_alive:
    runs-on: ubuntu-latest
    steps:
      - name: ping streamlit App
        run: curl https://convnn-appuction-6ohqw6d7njgf2bnytprl7e.streamlit.app/